<UserControl x:Class="WPFServiceSystem.DisplayQuestionWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFServiceSystem"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <ItemsControl ItemsSource="{Binding QNA}"/>
            <Button Content="Save" HorizontalAlignment="Center" Width="250" Height="80" FontSize="24" FontWeight="Bold" BorderBrush="#FFFF3B3F" Background="#FFFF3B3F" Foreground="White" Margin="0 20" Command="{Binding Save}"/>
        </StackPanel>
    </Grid>
</UserControl>
